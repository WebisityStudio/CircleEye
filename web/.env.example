# Circle Overwatch Web - Environment Variables
# Copy this file to .env and fill in your values
# 
# IMPORTANT:
# - Variables prefixed with VITE_ are exposed to the client-side code
# - They are NOT secrets - anything in the client can be extracted
# - Protect access with backend rules (e.g., Supabase RLS) or provider-side key restrictions
# - Real secrets must live on a server (your backend or Supabase Edge Functions)

# ---------------------------------------------
# Supabase (anon key is public by design; enforce RLS!)
# ---------------------------------------------
# Get these values from your Supabase project dashboard: Settings > API
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# ---------------------------------------------
# Backend base URL (if you have your own API)
# ---------------------------------------------
# Use HTTPS in production
VITE_BACKEND_URL=https://api.your-backend.com

# ---------------------------------------------
# Google Maps API
# Required for postcode/geocoding lookups
# Restrict this key in Google Cloud Console by:
# - HTTP referrers (websites) for web apps
# - API restrictions (enable only Geocoding API)
# Get your key: https://console.cloud.google.com/google/maps-apis
# ---------------------------------------------
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# ---------------------------------------------
# UK Public Data APIs
# These are public endpoints - no API keys required for basic usage
# Override only if needed for local development or testing
# ---------------------------------------------

# GOV.UK Threat Level API
# Official terrorism threat level from MI5
VITE_GOVUK_THREAT_ENDPOINT=https://www.gov.uk/api/content/terrorism-national-threat-level

# Police UK API (Street-level crime data)
# No authentication required
# Docs: https://data.police.uk/docs/
VITE_POLICE_API_BASE=https://data.police.uk/api

# Environment Agency Flood Monitoring API
# Real-time flood warnings and alerts
# Docs: https://environment.data.gov.uk/flood-monitoring/doc/reference
VITE_ENV_AGENCY_FLOOD_ENDPOINT=https://environment.data.gov.uk/flood-monitoring/id/floods

# ---------------------------------------------
# Optional: Met Office DataHub
# Requires registration at https://datahub.metoffice.gov.uk/
# ---------------------------------------------
# VITE_MET_OFFICE_DATAHUB_KEY=your-metoffice-api-key

# ---------------------------------------------
# App Configuration
# ---------------------------------------------
# App version (displayed in About page)
VITE_APP_VERSION=1.0.0

# Referral base URL (used in Refer a Friend feature)
VITE_REFERRAL_BASE_URL=https://circleoverwatch.com/signup

# ---------------------------------------------
# Development helpers
# ---------------------------------------------
# When true, route certain web requests via an Edge Function proxy
# VITE_USE_PROXY=false
